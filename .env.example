# FinTwin AI — variáveis de ambiente (VS-01)

# PostgreSQL
POSTGRES_USER=fintwin
POSTGRES_PASSWORD=fintwin
POSTGRES_DB=fintwin
POSTGRES_PORT=5432

# API
API_PORT=8000
DATABASE_URL=postgresql+psycopg://fintwin:fintwin@db:5432/fintwin

# Web
WEB_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:8000
