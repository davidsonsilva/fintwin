APPROVED_WITH_WARNINGS: a remoção de `simulate` de `available_actions` evita anunciar uma ação sem fluxo executável, preserva `save`/visualização e não introduz migrações, mudanças financeiras ou problemas de lint no arquivo alterado. Comandos executados: `git diff`, `git diff --stat`, `git show --name-only`, `git log -10`, `git diff --check` e `npx eslint src/features/agent/OpportunityCard.tsx` saíram 0; `pytest -p no:cacheprovider` foi NOT_VERIFIED porque a `.venv` aponta para um Python 3.12 inexistente, `npx vitest run` foi NOT_VERIFIED por `EPERM` ao criar diretório temporário, `npx tsc --noEmit --incremental false` e `npm run build` saíram 1 somente por TS2322/TS2345/TS2769 em arquivos de onboarding fora do diff, e `npx eslint .` saiu 1 somente por `react-hooks/immutability` e `@typescript-eslint/no-explicit-any` fora do diff. Critérios: 1-6 NOT_APPLICABLE ao diff atual; 7-14 PASS por inspeção estática sem regressão; 15 NOT_VERIFIED pelos testes bloqueados no ambiente; 16 PASS; risco residual/teste ausente: adicionar um teste de UI para mensagens legadas que ainda contenham `simulate`, e a próxima ação é repetir pytest/vitest em ambiente com Python/temporário graváveis.
