APPROVED_WITH_WARNINGS: a migração agora preserva compatibilidade com bancos que já possuem `agent_messages.confirmed`, sem alterar regras do agente. `git diff --check` e `docker compose config --quiet` passaram; `tsc` e lint reproduziram somente falhas PRE_EXISTING_FAILURE da baseline, enquanto pytest/Alembic, Vitest/build e o fluxo Docker ficaram NOT_VERIFIED por limitações do ambiente sandbox.
