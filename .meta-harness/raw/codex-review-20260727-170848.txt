APPROVED_WITH_WARNINGS — o diff restaura explicitamente o hover dos cards migrados, não altera domínio/API/schema e não introduz defeitos funcionais identificáveis por inspeção. Evidências executadas: `git diff`, `git diff --stat`, `git show --name-only`, `git log -10` e `git diff --check` (exit 0); `npx tsc --noEmit` (exit 2: apenas os 5 diagnósticos baseline TS2322, TS2345 e TS2769, mais TS5033 por escrita bloqueada no sandbox); `npm run lint` (exit 1: apenas 24 ocorrências baseline de @typescript-eslint/no-explicit-any); `npx vitest run` (NOT_VERIFIED, exit 1: sandbox não resolve `vitest.setup.ts` pelo caminho `/@fs`); pytest via `.venv` (NOT_VERIFIED, exit 103: Python 3.12 configurado ausente); e `npm run build` (NOT_VERIFIED, exit 1: EPERM ao abrir `.next/trace`). Critérios: C1 PASS, C2 NOT_VERIFIED, C3 PASS, C4 NOT_VERIFIED, C5 PASS por inspeção estática, C6 PASS, C7 PASS por comparação manual dos tokens e C8 PASS; risco residual é a falta de execução dinâmica de Vitest/build/rotas neste sandbox, não há testes específicos para o hover restaurado, e a próxima ação é executar Vitest, pytest e build em ambiente gravável com o venv configurado.
