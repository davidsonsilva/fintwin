The frontend production build, typecheck, and lint all fail. The decision simulator also has correctness gaps in loans, total-cost calculation, custom-scenario persistence/configuration, and request validation; backend pytest and Docker migration/manual-flow verification were blocked because the committed venv references an unavailable Python 3.12 installation and Docker access is unavailable, while Vitest was blocked by sandbox TEMP permissions.

Full review comments:

- [P1] Guard the chart tooltip formatter against undefined — D:\IA Projects\gemeo-financeiro\apps\web\src\features\dashboard\ProjectionChart.tsx:99-99
  HIGH: `npm run build` and `npx tsc --noEmit` fail because Recharts may call this formatter with an undefined value, while the callback requires `number`. This blocks the production frontend build.

- [P1] Accept the Select null value before updating state — D:\IA Projects\gemeo-financeiro\apps\web\src\features\fragility\FragilityList.tsx:40-40
  HIGH: `npx tsc --noEmit` rejects passing `setSeverity` directly because this Select can emit `null` while the state setter only accepts strings. The frontend typecheck remains broken after fixing the chart error.

- [P1] Align the profile form resolver input type — D:\IA Projects\gemeo-financeiro\apps\web\src\features\onboarding\ProfileStep.tsx:27-27
  HIGH: `npx tsc --noEmit` reports that the Zod resolver's input has `dependents: unknown`, but the form is declared with `dependents: number`. This prevents the frontend typecheck and production build from succeeding.

- [P1] Type the resource form schema consistently — D:\IA Projects\gemeo-financeiro\apps\web\src\features\onboarding\ResourceStepForm.tsx:117-117
  HIGH: the generic schema type supplied by `ResourceStepConfig` is incompatible with `zodResolver`, producing a TypeScript error at this call. This is another independent typecheck failure that must be resolved for a buildable frontend.

- [P1] Record the loan disbursement in the simulated cash flow — D:\IA Projects\gemeo-financeiro\apps\api\src\domain\decisions\appliers.py:162-166
  HIGH: for `LOAN`, this delegates to financing and only adds repayment debt; it never adds the borrowed principal as an income/event or account increase. A cash loan therefore immediately worsens the simulated balance instead of representing the funds received, yielding an incorrect financial comparison.

- [P1] Calculate recurring costs over their applicable duration — D:\IA Projects\gemeo-financeiro\apps\api\src\domain\decisions\engine.py:99-107
  HIGH: financing recurring costs are labeled as monthly in the form, but this total adds each amount only once. For example, R$180/month over 48 installments is reported as R$180 rather than the recurring cost over the chosen term, so the displayed `Custo total` is materially understated.

- [P1] Validate decision-specific parameters before simulation — D:\IA Projects\gemeo-financeiro\apps\api\src\interfaces\http\schemas\simulation.py:31-35
  HIGH: the request accepts an arbitrary parameter map and no code applies the registered required parameters or numeric bounds. The default form can submit an empty cash purchase, which reaches `parameters["amount"]` and raises a server error; negative amounts, invalid installment counts, and invalid dates can also produce nonsensical simulations or 500 responses instead of a 422 validation error.

- [P1] Persist custom scenario inputs with each simulation — D:\IA Projects\gemeo-financeiro\apps\api\src\application\use_cases\simulation_use_cases.py:70-75
  HIGH: the use case receives `scenario_override` but stores only the decision parameters. Consequently a simulation created with custom multipliers cannot be audited or reconstructed from its history/detail response, violating the requirement that custom-scenario parameters be persisted with the simulation.

- [P1] Expose every custom-scenario parameter in the form — D:\IA Projects\gemeo-financeiro\apps\web\src\features\simulation\DecisionForm.tsx:147-148
  HIGH: when users enable the customized scenario, the UI exposes only income, essential/nonessential expense multipliers, and unexpected expense. It omits the required income-loss duration and expense-reduction capacity, so the full ScenarioOverride contract from Spec 10.4 cannot be configured through the delivered visual simulator.

- [P2] Remove the untyped onboarding resource configuration — D:\IA Projects\gemeo-financeiro\apps\web\src\features\onboarding\resourceConfigs.ts:37-37
  MEDIUM: `npm run lint` fails on this and the other `ResourceStepConfig<any>` declarations, plus related `any` casts in the resource form. The project’s configured lint command therefore cannot pass and the onboarding data path remains unchecked by TypeScript.
