# FinTwin AI — Arquitetura (Graphify)

Não há relatório Graphify (`graphify-out/GRAPH_REPORT.md`) disponível — o repositório ainda não possui código de produto para analisar (apenas scaffold do `ai-dev-template`). Gerar/atualizar esta memória após a VS-01 criar a estrutura `apps/web` e `apps/api`.
