# Planejamento em Andamento

## ESTADO ATUAL / PRÓXIMO PASSO

**Task especificada e entregue no `ai-dev-template`. Nada implementado.**

Artefatos criados (fora deste projeto, no scaffold):
- `D:\IA Projects\ai-dev-template\TASK-portao-de-entrada-memoria.md` — spec completa
- `D:\IA Projects\ai-dev-template\.serena\memories\planning\portao-de-entrada-memoria_20260731.md`
  — memória-ponteiro para a task

O usuário vai executar **de outro PowerShell**, a partir do `ai-dev-template`.

**Neste projeto (`gemeo-financeiro`) nada foi implementado e nada deve ser** até a task rodar.
Pendência anterior intacta: `IconChip` + `StatusCard` prontos e **sem commit** (working tree
suja). Ver `mem:planning/temp_card-variantes-e-grid-indicadores_20260731`.

---

## Tarefa: Arrumar a casa — fazer a memória CHEGAR ao agente

## Iniciado em: 2026-07-31 11:54 | Status: TASK ENTREGUE, AGUARDANDO EXECUÇÃO NO SCAFFOLD

---

## ✅ Decisões Tomadas

1. **Escopo**: consertar a chegada da memória (sistema), não os cards do dashboard.
   Cards ficam para depois — são justamente o que o sistema consertado deve proteger.
2. **Nada global é editado a partir de outro projeto.** Regra pré-existente do usuário que eu
   não conhecia: toda melhoria de stack passa pelo `ai-dev-template`. Por isso a entrega foi
   uma *task*, não uma implementação.
3. **Não mexer no `serena-hooks.exe`** — binário. Adicionar hooks ao lado.
4. **Portão entrega conteúdo, não instrução.** O índice vai dentro da mensagem de bloqueio.
   Placa vs. catraca; só catraca conta.
5. **Bloquear uma vez por sessão**, não sempre — entrega garantida, deadlock impossível.
   (Decidi eu, com justificativa na task; usuário não chegou a responder a pergunta.)
6. **Validação será feita aqui**, no `gemeo-financeiro`, por ser o projeto do caso real.

---

## 🔍 LEVANTAMENTO (feito, com evidência — não redescobrir)

### Hooks globais `~/.claude/settings.json` — 8 entradas, 5 eventos

| evento | matcher | comando |
|---|---|---|
| PreToolUse | `Bash` | `rtk hook claude` |
| PreToolUse | `""` (toda ferramenta) | `serena-hooks remind` + `hook-relay.js` |
| PreToolUse | `mcp__serena__*` | `serena-hooks auto-approve` |
| PostToolUse | `write_memory\|edit_memory` | `touch-memory-marker.ps1` |
| PreCompact | `""` | `check-memory-marker.ps1` (**bloqueia**) |
| SessionStart | `""` | `serena-hooks activate` + `clear-memory-marker.ps1` |
| SessionEnd | `""` | `serena-hooks cleanup` (async) |

### 🔑 DESCOBERTA CENTRAL — assimetria escrita/leitura

Portão de **saída** completo (SessionStart zera → PostToolUse de *write* marca → PreCompact
bloqueia). Portão de **entrada** inexistente: nada dispara em `read_memory`, nada checa antes
do primeiro `Edit`. **A automação codifica o mesmo viés do problema.** Não faltou mecanismo —
o mecanismo é maduro e está apontado para um lado só.

### `ai-dev-template` (scaffold)

- `template-manifest.psd1` é **fonte única**, lida por `init-novo-projeto.ps1` E
  `audit-projeto.ps1`. Artefato registrado lá ganha distribuição + enforcement em legado.
- `audit-projeto.ps1` já percorre `RequiredSerenaMemories` (~linha 107) checando presença.
- `.claude/commands/` do template = 3 comandos (discutir-tarefa, fechar-discussao, ver-plano),
  copiados para todo projeto via `StaticFolders`.
- **`/abrir-nova-sessao` NÃO é comando de projeto — é skill global**
  (`~/.claude/skills/abrir-nova-sessao/`). Quase reportei como "comando faltando"; verifiquei
  antes. Não repetir esse falso achado.
- **O `CLAUDE.md` do template é idêntico ao do `gemeo-financeiro`**, incluindo a regra 3 com
  válvula de escape. O defeito é de fábrica e está replicado em todo projeto criado.
- `serena-hooks` = binário compilado, 46.5K, em `~/.local/bin/`.
- Nenhum projeto tem hooks próprios; `.claude/settings.local.json` só tem `permissions`.

---

## 📝 Pontos Discutidos

### [11:54] Conflito de ferramenta no plan mode
`/discutir-tarefa` manda gravar no Serena; plan mode bloqueava escrita. **Avisei em vez de
simular gravação** e usei o arquivo de plano como ponte até o usuário liberar.
**Motivo**: coerente com o tema — memória que não chega é indistinguível de memória bem escrita.

### [12:0x] Li os hooks antes de propor
Derrubou minha própria recomendação anterior e produziu a descoberta da assimetria.
Era a 3ª vez na sessão que eu opinava sem ler; desta vez li primeiro.

### [12:2x] A task caiu no mesmo erro que descreve
Entreguei a spec como `.md` solto na raiz do scaffold. Como `/abrir-nova-sessao` carrega
**memórias do Serena**, ela não seria encontrada por esse comando. Criei a memória-ponteiro.
**Lição**: "onde o artefato vive" é parte da entrega, não detalhe de arrumação. Escrever
sobre chegada não imuniza contra falhar na chegada.

---

## ❌ Correções do Usuário

- **Erro**: tentativa e erro no CSS sem ler o planning existente (4 tentativas em `min-height`).
  **Correção**: *"Você nao está planejando as suas acoes, está na tentativa / erro! Isso gasta
  tokens e o processo fica caro a cada tentativa."*
  **Lição**: `list_memories` antes do primeiro Edit — incondicionalmente, e principalmente
  quando a tarefa chega vestida de bug.

- **Erro**: descrever o projeto como "três sistemas de card competindo" e os tokens `.ts` como
  "6 arquivos de código morto".
  **Correção**: *"então isso não é a finalização da migração de 27/07?"* — era. Escopo excluído
  foi decisão deliberada e documentada; tokens são espelho consumido via `@theme`
  (`globals.css:9`), não via import.
  **Lição**: código diz o que *é*; memória diz o que foi *decidido e por quê*.

- **Erro (meta)**: tratei duas correções anteriores ("pare de tentar as coisas", "aqui sempre
  montamos o planning primeiro") como ajustes pontuais, obedeci localmente e nunca perguntei
  *por que* estava acontecendo.
  **Lição**: correção repetida é sintoma de causa comum — investigar a causa, não só cumprir.

- **Erro**: afirmei *"você já tem o mecanismo mas usa hooks para uma coisa só"*.
  **Correção**: falso — 8 entradas, 5 eventos, uma com matcher `""`.
  **Lição**: eu tinha acabado de escrever um post-mortem sobre opinar sem ler, e opinei sem ler
  de novo, sobre um arquivo que eu podia abrir em 1 comando. **Diagnosticar o problema não
  imuniza contra o problema.**

- **Erro (comunicação)**: em *"preciso copiar e colar o que vc mudaria?"* entendi "copiar
  arquivos"; ele perguntava se precisava me devolver **minhas próprias recomendações**.
  **Correção**: *"eu escrevo achando que vc me entende como outra pessoa entenderia (...)
  Ainda nã me conhece mesmo kkk"*
  **Lição**: Davidson escreve em registro coloquial e comprimido, contando com contexto
  compartilhado. Frase ambígua → a leitura certa costuma ser a que aponta para **o que acabou
  de ser dito na conversa**, não para uma entidade técnica do projeto.

- **Erro**: comecei a planejar mudanças em `~/.claude` a partir deste projeto.
  **Correção**: *"eu já tinha definido que se eu precisasse mexer em algo global, deveria usar
  o projeto ai-dev-template e nao através de outro projeto. Então, planeje e crie um task de
  melhoria, que eu rodo de lá!"*
  **Lição**: o `ai-dev-template` é o **único** ponto de entrada para mudança de stack. Existe
  também `audit-projeto.ps1` que equaliza projetos legados. Alteração global feita direto num
  projeto burla os dois e diverge do manifest.

---

## 🎯 Plano Final

Está na task, não aqui — para não divergir da fonte:
`D:\IA Projects\ai-dev-template\TASK-portao-de-entrada-memoria.md`

5 peças, nesta ordem: (1) `_index.md` + manifest, (2) regra do `CLAUDE.md`, (3) hooks do
portão, (4) template do `fechar-discussao`, (5) convenção de ponteiro no artefato.

**Critério de sucesso**: numa sessão nova, um agente que recebe tarefa disfarçada de bug numa
área com memória registrada **recebe essa memória antes de escrever a primeira linha de
código** — sem ninguém ter lembrado de invocar `/abrir-nova-sessao`.
